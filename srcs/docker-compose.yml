services:
  mariadb:
    build: ./requirements/mariadb
    container_name: mariadb
    env_file: .env
    networks:
      - inception
    volumes:
      - mariadb_data:/var/lib/mysql
    restart: always

networks:
  inception:
    driver: bridge

volumes:
  mariadb_data:
    driver: local
